package com.example.async.ui.theme

import androidx.compose.ui.graphics.Color

// Async Music Player Brand Colors (from colortheme guide)

// Primary brand colors
val AsyncPrimaryGreen = Color(0xFF1DB954)      // Primary Green - Main buttons, branding, progress bars
val AsyncLighterGreen = Color(0xFF1ED760)      // Lighter Green - Hover states, gradients
val AsyncBlack = Color(0xFF191414)             // Black - App background, navigation
val AsyncWhite = Color(0xFFFFFFFF)             // White - Text on dark backgrounds
val AsyncGray = Color(0xFF535353)              // Gray - Secondary text, borders, card backgrounds
val AsyncBlueAccent = Color(0xFF509BF5)        // Blue Accent - Selection indicators, progress messages

// Extended palette for Material3 design system
val AsyncSurfaceDark = Color(0xFF1E1E1E)       // Cards, dialogs, elevated surfaces
val AsyncSurfaceVariant = Color(0xFF2A2A2A)    // Input fields, settings panels
val AsyncOnSurfaceVariant = Color(0xFFB0B0B0)  // Tertiary text, placeholders
val AsyncError = Color(0xFFCF6679)             // Error states, delete actions

// Surface hierarchy colors
val AsyncSurfaceDim = Color(0xFF111111)        // Dimmed surfaces
val AsyncSurfaceBright = Color(0xFF2A2A2A)     // Bright surface variant
val AsyncOutlineVariant = Color(0xFF404040)    // Subtle borders

/**
 * Usage Guidelines:
 * 
 * Primary Green (#1DB954):
 * - Main action buttons (Play, Shuffle, Add to Playlist)
 * - App branding and logo elements
 * - Progress bars and active states
 * 
 * Lighter Green (#1ED760):
 * - Hover states and interactive feedback
 * - Gradient accents with Primary Green
 * 
 * Black (#191414):
 * - Primary app background
 * - Navigation backgrounds
 * - Text on light elements
 * 
 * White (#FFFFFF):
 * - Primary text on dark backgrounds
 * - High contrast elements
 * 
 * Gray (#535353):
 * - Secondary text and descriptions
 * - Borders and dividers
 * - Card backgrounds in dark theme
 * 
 * Blue Accent (#509BF5):
 * - Selection indicators
 * - Progress messages
 * - Secondary highlights
 */