package com.example.async.ui.components

import dagger.Module
import dagger.hilt.InstallIn
import dagger.hilt.components.SingletonComponent

/**
 * Hilt module for UI components in the Async music player.
 * Currently serves as a placeholder for future component-specific dependencies.
 */
@Module
@InstallIn(SingletonComponent::class)
object ComponentModule {
    // This module is ready for future component-specific dependencies
    // such as image loading, text processing, or other UI utilities
} 